{
  "name": "moviestream-mongo",
  "version": "1.0.0",
  "description": "MovieStream domain modeled in MongoDB with a minimal CRUD web app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "seed": "node seed.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^6.8.0"
  }
}
